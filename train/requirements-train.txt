# Extra packages needed only for TRAINING a wake word.
# The speaker itself doesn't need any of these.
#
#     source .venv/bin/activate
#     pip install -r train/requirements-train.txt
#
# This is a big install (~2 GB, mostly PyTorch).

# Makes the training clips. This is the piece that replaces
# piper-sample-generator, which has no build for Apple Silicon.
piper-tts>=1.7.0

# The trainer itself
torch>=2.2.0
torchaudio>=2.2.0
torchcodec          # torchaudio 2.11+ loads audio through this
torchinfo
torchmetrics

# Used by openWakeWord's data pipeline. It doesn't declare these itself,
# so they have to be installed by hand.
pronouncing
mutagen
acoustics
audiomentations
torch-audiomentations
speechbrain
deep-phonemizer

# Saving the finished model
onnx
onnxscript          # torch's ONNX exporter needs this

# NOT needed: tensorflow, onnx_tf, tflite-runtime.
# Those are only for converting the model to TensorFlow Lite, which we skip
# — the speaker loads the .onnx file directly.
